import {{project.projectType}} from '../src/{{project.projectType}}.{{project.target}}.js';

describe('{{project.projectType}}', () => {
  it('should be truthy', () => {
    expect({{project.projectType}}).toBeTruthy();
  });
});
