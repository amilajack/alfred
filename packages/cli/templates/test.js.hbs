import {{project.project}} from '../src/{{project.project}}.{{project.platform}}';

describe('{{project.project}}', () => {
  it('should be truthy', () => {
    expect({{project.project}}).toBeTruthy();
  });
});
