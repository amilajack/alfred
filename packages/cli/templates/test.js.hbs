import {{project.projectType}} from '../src/{{project.projectType}}.{{project.target}}';

describe('{{project.projectType}}', () => {
  it('should be truthy', () => {
    expect({{project.projectType}}).toBeTruthy();
  });
});
