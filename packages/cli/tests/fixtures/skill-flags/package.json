{
  "name": "@fixtures/skill-flags",
  "version": "0.0.2",
  "private": true,
  "license": "MIT",
  "scripts": {
    "_build": "../../../../cli/lib/commands/alfred.js run build",
    "build": "yarn _build && yarn _build --production && yarn _build --prod && yarn _build --development && yarn _build --dev && yarn _build --test",
    "clean": "../../../../cli/lib/commands/alfred.js run clean",
    "format": "../../../../cli/lib/commands/alfred.js run format",
    "lint": "../../../../cli/lib/commands/alfred.js run lint --env browser,node --format pretty",
    "skills": "../../../../cli/lib/commands/alfred.js skills",
    "start": "../../../../cli/lib/commands/alfred.js run start",
    "test": "../../../../cli/lib/commands/alfred.js run test",
    "test-all": "yarn clean && yarn build && yarn lint && yarn format && yarn skills && yarn test"
  },
  "dependencies": {
    "@babel/preset-env": "^7.8.3"
  },
  "devDependencies": {
    "@alfred/cli": "^0.0.2",
    "@alfred/core": "^0.0.2",
    "eslint-formatter-pretty": "^3.0.1"
  },
  "alfred": {
    "showConfigs": true
  }
}
