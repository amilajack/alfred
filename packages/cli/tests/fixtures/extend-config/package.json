{
  "name": "@fixtures/extend-config",
  "version": "0.0.3",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "../../../../cli/lib/commands/alfred.js run build",
    "clean": "../../../../cli/lib/commands/alfred.js run clean",
    "format": "../../../../cli/lib/commands/alfred.js run format",
    "lint": "../../../../cli/lib/commands/alfred.js run lint",
    "skills": "../../../../cli/lib/commands/alfred.js skills",
    "start": "../../../../cli/lib/commands/alfred.js run start",
    "test": "../../../../cli/lib/commands/alfred.js run test",
    "test-all": "yarn clean && yarn build && yarn lint && yarn format && yarn skills && yarn test"
  },
  "devDependencies": {
    "@alfred/cli": "^0.0.3",
    "@alfred/core": "^0.0.3",
    "@babel/core": "^7.8.3",
    "@examples/alfred-config-example": "^0.0.3"
  },
  "alfred": {
    "extends": "@examples/alfred-config-example",
    "showConfigs": true
  }
}
