{
  "name": "@alfredpkg/core",
  "version": "0.0.0",
  "main": "./lib/index.js",
  "scripts": {
    "build": "cross-env NODE_ENV=production babel src --out-dir lib --source-maps inline",
    "build-watch": "cross-env NODE_ENV=production babel src --out-dir lib --watch --source-maps inline",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "@alfredpkg/skill-babel": "^0.0.0",
    "@alfredpkg/skill-eslint": "^0.0.0",
    "@alfredpkg/skill-jest": "^0.0.0",
    "@alfredpkg/skill-prettier": "^0.0.0",
    "@alfredpkg/skill-webpack": "^0.0.0",
    "@alfredpkg/skill-react": "^0.0.0",
    "lodash": "^4.17.11",
    "rimraf": "^2.6.2",
    "webpack-merge": "^4.1.5"
  }
}
