{
  "packages": [
    "examples/*",
    "packages/*",
    "tests/fixtures/*",
    "website"
  ],
  "version": "0.0.0",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "scripts": {
    "lint": "eslint --cache --format=eslint-formatter-pretty .",
    "lint-fix": "yarn --silent lint --fix"
  },
  "command": {
    "run": {
      "npmClient": "yarn"
    }
  }
}
