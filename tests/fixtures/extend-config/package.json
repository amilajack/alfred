{
  "name": "@fixtures/extend-config",
  "version": "0.0.2-alpha.4",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "../../../packages/cli/lib/commands/alfred.js run build",
    "clean": "../../../packages/cli/lib/commands/alfred.js run clean",
    "format": "../../../packages/cli/lib/commands/alfred.js run format",
    "lint": "../../../packages/cli/lib/commands/alfred.js run lint",
    "skills": "../../../packages/cli/lib/commands/alfred.js skills",
    "start": "../../../packages/cli/lib/commands/alfred.js run start",
    "test": "../../../packages/cli/lib/commands/alfred.js run test",
    "test-all": "yarn clean && yarn build && yarn lint && yarn format && yarn skills && yarn test"
  },
  "devDependencies": {
    "@alfred/cli": "^0.0.2-alpha.4",
    "@alfred/core": "^0.0.2-alpha.4",
    "@babel/core": "^7.7.4",
    "@examples/alfred-config-example": "^0.0.2-alpha.4"
  },
  "alfred": {
    "extends": "@examples/alfred-config-example",
    "showConfigs": true
  }
}
